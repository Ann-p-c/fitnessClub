$(function () {
  function o(o) {
    o.find(".modal__inner").css({ transform: "scale(0.5)", opacity: "0" }),
      setTimeout(function () {
        $("body").removeClass("lock"), $(o).removeClass("active");
      }, 200);
  }
  $(".slider-about").slick({ slidesToShow: 1, arrows: !1, dots: !0, slidesToShow: 1, slidesToScroll: 1, autoplay: !1, speed: 400, adaptiveHeight: !0, fade: !0 }),
    $(".slider-team").slick({ slidesToShow: 1, arrows: !1, dots: !0, slidesToShow: 1, infinite: !0, autoplay: !0, autoplaySpeed: 3e3, speed: 300, centerMode: !0, variableWidth: !0 }),
    $("[data-scroll]").on("click", function (o) {
      o.preventDefault();
      let e = $(this).data("scroll"),
        a = $(e).offset().top;
      $(".burger").removeClass("active"), $(".menu").removeClass("active"), $("body").removeClass("lock"), $("body,html").animate({ scrollTop: a - $(".header").innerHeight() - 20 }, 600);
    }),
    $(window).on("scroll", function (o) {
      o.preventDefault();
      let e = $(this).scrollTop();
      $("[data-scrollspy]").each(function () {
        let o = $(this).data("scrollspy"),
          a = $(this).offset().top;
        (a -= 0.3 * $(window).height()), e >= a ? ($("[data-scroll]").removeClass("active"), $("[data-scroll='" + o + "' ]").addClass("active")) : 0 == e && $("[data-scroll]").removeClass("active");
      }),
        $(this).scrollTop() > 10 ? $(".scroll-up").fadeIn() : $(".scroll-up").fadeOut();
    }),
    $("[data-modal]").on("click", function (o) {
      o.preventDefault();
      let e = $(this).data("modal");
      $("body").addClass("lock"),
        $(e).addClass("active"),
        setTimeout(function () {
          $(e).find(".modal__inner").css({ transform: "scale(1)", opacity: "1" });
        });
    }),
    $("[data-modal-close]").on("click", function (e) {
      e.preventDefault(), o($(this).parents(".modal"));
    }),
    $(".modal").on("click", function () {
      o($(this));
    }),
    $(".modal__inner").on("click", function (o) {
      o.stopPropagation();
    }),
    $(".burger").on("click", function (o) {
      $(".burger,.menu").toggleClass("active"), $("header").toggleClass("open"), $("body").toggleClass("lock");
    });
});
